spring.application.name=health-assistant
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/health_assistant1
spring.datasource.username=mathi
spring.datasource.password=Mysql@2004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Define the directory where images will be uploaded
upload.dir=./uploads/images

# MongoDB Connection Settings
spring.data.mongodb.uri=mongodb://localhost:27017/profileDB

# JWT Secret
jwt.secret=your_jwt_secret_key

# File Upload Path (for profile images)
file.upload-dir=uploads
